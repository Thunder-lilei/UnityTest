# C# / Unity / Lua 综合学习文档

> **项目路径**: `C:\Learn\UnityTest\Test\`
> **引擎版本**: Unity 2022.3.62t11 (Tuanjie)
> **项目类型**: 3D 生存战斗游戏（迷你游戏）
> **文档日期**: 2026-08-12
> **适用读者**: C# 刚入门，能看懂基本语法但独立编写有困难的学习者

---

## 目录

- [第一章 C# 基础篇](#第一章-c-基础篇)
- [第二章 C# 进阶篇](#第二章-c-进阶篇)
- [第三章 Unity C# 实战篇](#第三章-unity-c-实战篇)
- [第四章 性能优化篇](#第四章-性能优化篇)
- [第五章 设计模式篇](#第五章-设计模式篇)
- [第六章 Lua 基础与热更篇](#第六章-lua-基础与热更篇)
- [第七章 面试准备篇](#第七章-面试准备篇)
- [第八章 实践任务篇](#第八章-实践任务篇)

---

## 第一章 C# 基础篇

### 1.1 变量与类型

#### 1.1.1 值类型 vs 引用类型

这是 C# 最核心的概念之一，直接影响内存分配和性能。

| 类别 | 存储位置 | 赋值行为 | 示例 |
|---|---|---|---|
| **值类型** | 栈（Stack） | 复制值 | int, float, bool, struct, Vector3 |
| **引用类型** | 堆（Heap）+ 栈中存引用 | 复制引用（指向同一对象） | class, string, array |

```csharp
// 值类型：复制值
int a = 10;
int b = a;    // b 复制了 a 的值
b = 20;       // 改 b 不影响 a
// a = 10, b = 20

// 引用类型：复制引用
int[] arr1 = { 1, 2, 3 };
int[] arr2 = arr1;   // arr2 和 arr1 指向同一个数组
arr2[0] = 99;         // 改 arr2 也改了 arr1
// arr1[0] = 99
```

#### 项目中的实际例子

```csharp
// PlayerController.cs — Vector3 是 struct（值类型）
Vector3 movement = new Vector3(movementX, 0.0f, movementY);
// movement 存在栈上，不产生 GC

// EnemySpawner.cs — List<GameObject> 是 class（引用类型）
private List<GameObject> enemies = new List<GameObject>();
// enemies 引用存在栈上，实际数据在堆上
```

#### 1.1.2 装箱与拆箱

将值类型转为引用类型（object）会产生装箱，在堆上分配内存，**产生 GC**。

```csharp
// ❌ 装箱：int → object，堆上分配内存
int x = 10;
object o = x;       // 装箱（Boxing）

// ❌ 拆箱：object → int，从堆上取回
int y = (int)o;     // 拆箱（Unboxing）

// 游戏中要避免的装箱场景
ArrayList list = new ArrayList();
list.Add(1);        // 装箱！int → object

// ✅ 正确做法：使用泛型集合
List<int> list2 = new List<int>();
list2.Add(1);       // 无装箱
```

#### 1.1.3 string 的特殊性

`string` 是引用类型，但表现像值类型 —— **不可变（immutable）**。

```csharp
string s1 = "Hello";
string s2 = s1;       // s2 和 s1 指向同一个 "Hello"
s2 = "World";         // s2 指向新的 "World"，s1 不变

// ❌ 每次拼接都产生新字符串，GC 压力大
string result = "";
for (int i = 0; i < 100; i++)
    result += i.ToString();  // 每次创建新 string 对象

// ✅ 使用 StringBuilder
System.Text.StringBuilder sb = new System.Text.StringBuilder();
for (int i = 0; i < 100; i++)
    sb.Append(i.ToString());
string result2 = sb.ToString();
```

#### 项目中的字符串使用

```csharp
// ExpBar.cs — 简单拼接，频率低，可以接受
levelText.text = "Lv. " + level.ToString();

// EnemySpawner.cs — 使用 string.Format，比 + 拼接稍好
return string.Format("{0:00}:{1:00}", minutes, seconds);

// 如果在高频更新中拼接字符串，应改用 StringBuilder
```

---

### 1.2 类与对象

#### 1.2.1 类的基本结构

```csharp
public class HealthBar : MonoBehaviour   // 继承
{
    // 字段（Field）— 类的内部数据
    public float maxHealth = 100f;         // public：外部可访问，Inspector 可编辑
    private float currentHealth;           // private：仅类内部可访问

    // 属性（Property）— 控制对字段的访问
    // public float CurrentHealth { get { return currentHealth; } set { currentHealth = value; } }

    // 方法（Method）— 类的行为
    public void TakeDamage(float damage)   // public 方法，外部可调用
    {
        currentHealth = Mathf.Max(0f, currentHealth - damage);
        UpdateFill();                      // 调用私有方法
    }

    private void UpdateFill()              // private 方法，仅类内部调用
    {
        if (fillRect != null)
        {
            float ratio = currentHealth / maxHealth;
            fillRect.anchorMax = new Vector2(ratio, fillRect.anchorMax.y);
        }
    }
}
```

#### 1.2.2 访问修饰符

| 修饰符 | 可见范围 | 项目中的使用场景 |
|---|---|---|
| `public` | 所有代码 | Inspector 需要拖拽的字段、外部需调用的方法 |
| `private` | 仅类内部 | 缓存的组件引用、内部状态变量 |
| `protected` | 类及子类 | 基类中供子类重写的方法 |
| `internal` | 同一程序集 | 工具类、扩展方法 |

```csharp
// PlayerController.cs 的字段设计原则
public float speed = 10f;              // public：Inspector 可调 + 升级系统可修改
public GameObject gameOverPanel;       // public：Inspector 拖拽
private Rigidbody rb;                 // private：组件缓存，外部不需要访问
private bool isPaused = false;        // private：内部状态，外部通过 SetPaused() 修改
```

#### 1.2.3 构造函数与初始化

```csharp
// C# 的构造函数
public class Enemy
{
    public float maxHealth;
    public float speed;

    // 构造函数
    public Enemy(float health, float moveSpeed)
    {
        maxHealth = health;
        speed = moveSpeed;
    }
}

// 使用
Enemy zombie = new Enemy(2f, 2.5f);
```

> **Unity 特殊规则**：MonoBehaviour 不能手动 `new`，只能 `AddComponent<T>()` 或挂在 GameObject 上。Unity 自动调用 `Awake()` 和 `Start()` 代替构造函数。

```csharp
// ❌ 错误：不能 new MonoBehaviour
PlayerController player = new PlayerController();

// ✅ 正确：通过 GameObject 获取
PlayerController player = gameObject.GetComponent<PlayerController>();

// ✅ 正确：通过 AddComponent 添加
PlayerController player = gameObject.AddComponent<PlayerController>();
```

---

### 1.3 继承与多态

#### 1.3.1 继承基础

```csharp
// 基类
public class Character : MonoBehaviour
{
    protected float health;          // protected：子类可访问

    public virtual void TakeDamage(float damage)   // virtual：允许子类重写
    {
        health -= damage;
    }
}

// 子类
public class Player : Character
{
    public override void TakeDamage(float damage)   // override：重写基类方法
    {
        // 可以扩展基类行为
        base.TakeDamage(damage);    // 先调用基类逻辑
        // 再添加玩家特有的逻辑
        AudioManager.Instance?.PlayPlayerHurt();
    }
}
```

#### 1.3.2 抽象类与抽象方法

```csharp
// 抽象类：不能实例化，只能被继承
public abstract class EnemyBase : MonoBehaviour
{
    public abstract void Attack();              // 抽象方法：子类必须实现
    public abstract float GetSpeed();           // 抽象属性/方法

    // 普通方法：子类可以直接用
    public void MoveToPlayer()
    {
        // 通用移动逻辑
    }
}

// 子类必须实现所有抽象方法
public class Zombie : EnemyBase
{
    public override void Attack()
    {
        // 僵尸特有的攻击方式
    }

    public override float GetSpeed()
    {
        return 2.5f;
    }
}
```

#### 1.3.3 项目中的多态实例

```csharp
// ObjectPool.cs — IPooledObject 接口实现多态
public interface IPooledObject
{
    void OnSpawn();    // 接口方法：从池中取出时调用
}

// Fireball.cs 实现接口
public class Fireball : MonoBehaviour, IPooledObject
{
    public void OnSpawn()              // 接口实现
    {
        timer = 0f;
        if (vfx != null) { vfx.Reinit(); vfx.Play(); }
    }
}

// Footprint.cs 也可以实现接口
public class Footprint : MonoBehaviour, IPooledObject
{
    public void OnSpawn()
    {
        // 重置透明度等状态
    }
}

// ObjectPool 不需要知道具体类型，通过接口调用
public GameObject Spawn(Vector3 position, Quaternion rotation)
{
    // ...
    var pooled = obj.GetComponent<IPooledObject>();  // 获取接口引用
    if (pooled != null)
        pooled.OnSpawn();    // 多态调用：实际执行的是 Fireball 或 Footprint 的 OnSpawn
    return obj;
}
```

> **关键理解**：接口是一种"契约"。实现接口的类必须遵守这个契约，但可以各自有不同实现。调用者不需要知道具体类型，只需要知道"它实现了 IPooledObject"。

---

### 1.4 集合

#### 1.4.1 常用集合对比

| 集合 | 特点 | 项目中的使用 |
|---|---|---|
| `T[]` 数组 | 固定长度，最快 | `UpgradeType[] allTypes`（UpgradeSystem） |
| `List<T>` | 动态长度，按索引访问 | `List<GameObject> enemies`（EnemySpawner） |
| `Queue<T>` | 先进先出（FIFO） | `Queue<GameObject> pool`（ObjectPool） |
| `Dictionary<K,V>` | 键值对，O(1) 查找 | 升级类型映射 |
| `Stack<T>` | 后进先出（LIFO） | DFS 寻路 |

#### 1.4.2 List\<T> 详解

```csharp
// EnemySpawner.cs 的实际使用
private List<GameObject> enemies = new List<GameObject>();

// 添加
enemies.Add(enemy);                    // 添加到末尾
enemies.Insert(0, enemy);              // 插入到指定位置

// 删除
enemies.Remove(enemy);                 // 按引用删除（O(n) 查找）
enemies.RemoveAll(e => e == null);     // 删除所有满足条件的元素
enemies.RemoveAt(index);               // 按索引删除

// 查询
int count = enemies.Count;              // 元素数量
GameObject first = enemies[0];          // 按索引访问
bool exists = enemies.Contains(enemy);  // 是否包含

// 遍历
foreach (GameObject e in enemies)       // foreach 遍历
{
    if (e != null) { /* ... */ }
}
```

#### 1.4.3 Queue\<T> 详解

```csharp
// ObjectPool.cs 的实际使用
private Queue<GameObject> pool = new Queue<GameObject>();

// 入队（添加到队尾）
pool.Enqueue(obj);

// 出队（从队首取出）
GameObject obj = pool.Dequeue();

// 查看（不取出）
GameObject peek = pool.Peek();

// 检查
int count = pool.Count;
bool empty = pool.Count == 0;
```

#### 1.4.4 Dictionary 详解

```csharp
// 如果用 Dictionary 改造 UpgradeSystem 的升级映射
Dictionary<UpgradeType, string> upgradeTitles = new Dictionary<UpgradeType, string>()
{
    { UpgradeType.MaxHealth, "+ 最大血量" },
    { UpgradeType.Speed, "+ 移动速度" },
    { UpgradeType.FireballCount, "+ 火球数量" },
    { UpgradeType.MagnetRange, "+ 吸取范围" },
};

// 查找（O(1)）
string title = upgradeTitles[UpgradeType.Speed];

// 安全查找
if (upgradeTitles.TryGetValue(type, out string desc))
{
    // 找到了
}

// 遍历
foreach (var pair in upgradeTitles)
{
    Debug.Log($"类型: {pair.Key}, 标题: {pair.Value}");
}
```

---

### 1.5 枚举

```csharp
// UpgradeSystem.cs 中的枚举定义
public enum UpgradeType
{
    MaxHealth,       // 0
    Speed,           // 1
    FireballCount,   // 2
    MagnetRange      // 3
}

// 枚举可以作为数组索引
string[] titles = { "+ 最大血量", "+ 移动速度", "+ 火球数量", "+ 吸取范围" };
string title = titles[(int)UpgradeType.Speed];   // titles[1] = "+ 移动速度"

// 枚举可以转字符串
string name = UpgradeType.MaxHealth.ToString();   // "MaxHealth"

// 字符串转枚举
UpgradeType type = (UpgradeType)System.Enum.Parse(typeof(UpgradeType), "Speed");
```

---

### 1.6 控制流

#### 1.6.1 条件语句

```csharp
// if-else
if (movement.magnitude > 0.1f)
{
    // 有移动输入时转向
    Quaternion targetRotation = Quaternion.LookRotation(movement);
    transform.rotation = Quaternion.Slerp(transform.rotation, targetRotation, 0.1f);
}

// switch-case（UpgradeSystem.cs 中的实际代码）
switch (type)
{
    case UpgradeType.MaxHealth:
        healthBar.IncreaseMaxHealth(20f);
        break;
    case UpgradeType.Speed:
        player.speed += 2f;
        break;
    case UpgradeType.FireballCount:
        player.fireballCount += 1;
        break;
    case UpgradeType.MagnetRange:
        magnetDetector.IncreaseRadius(1f);
        break;
}
```

#### 1.6.2 循环语句

```csharp
// for 循环（ObjectPool.cs 的预创建逻辑）
for (int i = 0; i < initialSize; i++)
{
    GameObject obj = Instantiate(prefab, parent);
    obj.SetActive(false);
    pool.Enqueue(obj);
}

// while 循环（ExpBar.cs 的跨级升级）
while (currentExp >= maxExp)
{
    currentExp -= maxExp;
    level++;
    maxExp += 20f;
}

// foreach 循环（遍历集合）
foreach (GameObject enemy in enemies)
{
    if (enemy != null)
        Debug.Log(enemy.name);
}
```

#### 1.6.3 三元运算符与 null 合并

```csharp
// 三元运算符
float ratio = currentHealth > 0 ? currentHealth / maxHealth : 0f;

// null 条件运算符 ?.（PlayerController.cs 中的实际使用）
AudioManager.Instance?.PlayPlayerHurt();
// 等价于：if (AudioManager.Instance != null) AudioManager.Instance.PlayPlayerHurt();

// null 合并运算符 ??
Transform parent = GameObject.Find("PickUp")?.transform ?? transform;
// 如果 Find("PickUp") 为 null，使用 transform 作为父级
```

---

### 1.7 方法参数

#### 1.7.1 参数传递方式

```csharp
// 值传递（默认）— 修改不影响原始值
public void ModifyValue(int x)
{
    x = 100;    // 只改了副本
}
int a = 5;
ModifyValue(a);    // a 仍然是 5

// ref 传递 — 修改影响原始值
public void ModifyRef(ref int x)
{
    x = 100;    // 直接修改原始变量
}
int b = 5;
ModifyRef(ref b);    // b 变为 100

// out 传递 — 方法必须赋值，用于返回多个值
public void GetPosition(out float x, out float y)
{
    x = 1f;
    y = 2f;
}
float px, py;
GetPosition(out px, out py);    // px=1, py=2

// in 传递（C# 7.2+）— 只读引用传递，避免值类型复制开销
public void ProcessLargeStruct(in Vector3 bigVector)
{
    // 只能读，不能改 bigVector
}
```

#### 项目中的 out 参数使用

```csharp
// EnemySpawner.cs — NavMesh.SamplePosition 的 out 参数
if (NavMesh.SamplePosition(spawnPos, out NavMeshHit hit, 10f, NavMesh.AllAreas))
    return hit.position;

// PlayerController.cs — Physics.Raycast 的 out 参数
if (!Physics.Raycast(ray, out RaycastHit hit, 100f))
    return;
```

---

### 1.8 struct vs class

这是 Unity 中非常重要的选择，直接影响性能。

| 特性 | struct（值类型） | class（引用类型） |
|---|---|---|
| 存储位置 | 栈（或内联在引用类型中） | 堆 |
| 赋值行为 | 复制全部数据 | 复制引用 |
| GC 影响 | **无 GC** | 分配/回收产生 GC |
| 继承 | 不能继承（可实现接口） | 可以继承 |
| 适用场景 | 小型、轻量数据 | 复杂对象、需要引用语义 |

```csharp
// Unity 内置 struct（值类型，无 GC）
Vector3 position;       // 3个float
Quaternion rotation;    // 4个float
Color color;            // 4个float

// 项目中大量使用 struct，不会产生 GC
Vector3 movement = new Vector3(movementX, 0.0f, movementY);    // 栈上分配，无 GC

// class（引用类型，有 GC）
public class EnemyMovement : MonoBehaviour { ... }    // 堆上分配
```

> **经验法则**：数据量小（< 16 字节）、不需要引用语义 → 用 struct；数据量大、需要继承 → 用 class。

---

## 第二章 C# 进阶篇

### 2.1 泛型

#### 2.1.1 为什么需要泛型

```csharp
// 没有泛型：用 object 存储，会装箱
ArrayList list = new ArrayList();
list.Add(1);         // 装箱：int → object（GC！）
int value = (int)list[0];   // 拆箱

// 有泛型：类型安全，无装箱
List<int> list = new List<int>();
list.Add(1);         // 无装箱
int value = list[0];  // 无拆箱
```

#### 2.1.2 泛型方法

```csharp
// Unity 的 GetComponent<T> 就是泛型方法
Rigidbody rb = GetComponent<Rigidbody>();
HealthBar health = GetComponent<HealthBar>();

// 自定义泛型方法
public T GetComponentSafe<T>() where T : Component
{
    T component = GetComponent<T>();
    if (component == null)
        Debug.LogError($"Missing {typeof(T).Name} on {gameObject.name}");
    return component;
}
```

#### 2.1.3 泛型约束（where）

```csharp
// 约束泛型类型必须满足条件
public T CreateComponent<T>() where T : Component    // T 必须是 Component 或其子类
{
    return gameObject.AddComponent<T>();
}

// 常见约束
// where T : class        → T 必须是引用类型
// where T : struct       → T 必须是值类型
// where T : new()        → T 必须有无参构造函数
// where T : Component    → T 必须继承 Component
// where T : IInterface    → T 必须实现某接口
```

#### 项目中的泛型使用

```csharp
// ObjectPool.cs 使用 Queue<T>（泛型集合）
private Queue<GameObject> pool = new Queue<GameObject>();

// IPooledObject 接口与 GetComponent 配合
var pooled = obj.GetComponent<IPooledObject>();    // 泛型方法

// Unity API 中大量使用泛型
rb = GetComponent<Rigidbody>();
animator = GetComponent<Animator>();
healthBar = GetComponent<HealthBar>();
```

---

### 2.2 委托与事件

#### 2.2.1 委托基础

委托是**方法的引用**，可以将方法当作参数传递。

```csharp
// 1. 定义委托类型
public delegate void DamageHandler(float damage);

// 2. 创建委托实例
DamageHandler handler = OnDamageTaken;    // 将方法赋给委托

// 3. 调用委托
handler(20f);    // 等价于调用 OnDamageTaken(20f)

void OnDamageTaken(float damage)
{
    Debug.Log($"受到 {damage} 点伤害");
}
```

#### 2.2.2 内置委托类型（Action / Func）

C# 内置了常用的泛型委托，不需要自己定义：

```csharp
// Action — 无返回值
Action<string> logAction = (msg) => Debug.Log(msg);
logAction("Hello");

// Action<float> — 等价于 delegate void DamageHandler(float damage)
Action<float> damageAction = (damage) => { healthBar.TakeDamage(damage); };
damageAction(20f);

// Func — 有返回值
Func<int, int, int> addFunc = (a, b) => a + b;
int result = addFunc(3, 5);    // 8

// Predicate<T> — 返回 bool
Predicate<GameObject> isEnemy = (go) => go.CompareTag("Enemy");
bool result = isEnemy(someGameObject);
```

#### 2.2.3 事件（event）

`event` 是对委托的封装，限制外部只能 `+=`（订阅）和 `-=`（取消），不能直接赋值或调用。

```csharp
// 使用 event 的观察者模式
public class PlayerHealth : MonoBehaviour
{
    // event：外部只能 += / -=，不能直接调用
    public event Action<float> OnHealthChanged;
    public event Action OnPlayerDeath;

    public void TakeDamage(float damage)
    {
        currentHealth -= damage;
        OnHealthChanged?.Invoke(currentHealth);    // 通知所有订阅者

        if (currentHealth <= 0)
            OnPlayerDeath?.Invoke();                // 通知死亡事件
    }
}

// 订阅者：UI 血条
public class HealthBarUI : MonoBehaviour
{
    void Start()
    {
        var health = GetComponent<PlayerHealth>();
        health.OnHealthChanged += UpdateBar;        // 订阅事件
        health.OnPlayerDeath += ShowGameOver;
    }

    void OnDestroy()
    {
        var health = GetComponent<PlayerHealth>();
        if (health != null)
        {
            health.OnHealthChanged -= UpdateBar;     // 取消订阅（重要！）
            health.OnPlayerDeath -= ShowGameOver;
        }
    }

    void UpdateBar(float health) { /* 更新 UI */ }
    void ShowGameOver() { /* 显示面板 */ }
}
```

#### 委托 vs 事件对比

| 特性 | delegate | event |
|---|---|---|
| 外部赋值 `=` | ✅ 可以 | ❌ 不可以 |
| 外部订阅 `+=` | ✅ 可以 | ✅ 可以 |
| 外部调用 `()` | ✅ 可以 | ❌ 不可以 |
| 适用场景 | 回调函数 | 发布-订阅模式 |

#### 项目中的委托应用

```csharp
// UpgradeCard.cs — Unity UI Button 的 onClick 就是委托
button.onClick.AddListener(() => system.SelectUpgrade(type));

// 如果用 C# event 改造升级系统
public class UpgradeSystem : MonoBehaviour
{
    public event Action<UpgradeType> OnUpgradeSelected;    // 事件：升级被选择

    public void SelectUpgrade(UpgradeType type)
    {
        // ... 应用升级效果 ...
        OnUpgradeSelected?.Invoke(type);    // 通知订阅者
    }
}

// 成就系统可以订阅升级事件
upgradeSystem.OnUpgradeSelected += (type) =>
{
    if (type == UpgradeType.MaxHealth)
        achievementTracker.Unlock("health_booster");
};
```

---

### 2.3 Lambda 表达式

Lambda 是编写匿名方法的简写形式。

```csharp
// 匿名方法（旧写法，已不推荐）
Action<string> log1 = delegate(string msg) { Debug.Log(msg); };

// Lambda 表达式（推荐）
Action<string> log2 = (msg) => Debug.Log(msg);

// 多语句 Lambda
Action<int> process = (x) =>
{
    int result = x * 2;
    Debug.Log($"结果: {result}");
};

// Lambda 配合 LINQ
enemies.RemoveAll(e => e == null);    // 删除所有 null 引用

// Lambda 配合排序
var sorted = enemies.OrderBy(e => Vector3.Distance(e.transform.position, player.position));
```

#### 闭包陷阱

Lambda 捕获变量时，引用的是变量本身，不是值。

```csharp
// ❌ 闭包陷阱：所有 Lambda 都引用同一个 i
for (int i = 0; i < 3; i++)
{
    button.onClick.AddListener(() => Debug.Log(i));
}
// 点击任何按钮都输出 3（因为循环结束时 i=3）

// ✅ 修复：在循环内创建局部变量
for (int i = 0; i < 3; i++)
{
    int captured = i;    // 每次迭代创建新变量
    button.onClick.AddListener(() => Debug.Log(captured));
}
// 分别输出 0, 1, 2
```

---

### 2.4 LINQ

LINQ（Language Integrated Query）提供对集合的查询能力。

```csharp
using System.Linq;

// 项目中使用 LINQ（EnemySpawner.cs — 虽然用的是 RemoveAll）
enemies.RemoveAll(e => e == null);

// 常用 LINQ 方法
var aliveEnemies = enemies.Where(e => e != null);                    // 过滤
var count = enemies.Count(e => e.activeSelf);                        // 计数
var nearest = enemies.OrderBy(e => Vector3.Distance(e.transform.position, player.position)).FirstOrDefault();  // 排序取第一个
var positions = enemies.Select(e => e.transform.position).ToArray();  // 映射

// 检查是否满足条件
bool anyAlive = enemies.Any(e => e != null && e.activeSelf);
bool allDead = enemies.All(e => e == null);

// 聚合
float totalHealth = enemies.Where(e => e != null).Sum(e => e.GetComponent<EnemyMovement>().maxHealth);
```

#### ⚠️ LINQ 的性能问题

```csharp
// ❌ 在 Update 中使用 LINQ（每帧产生 GC）
void Update()
{
    var alive = FindObjectsOfType<EnemyMovement>()
        .Where(e => e.currentHealth > 0)
        .OrderBy(e => e.currentHealth);
    // FindObjectsOfType 本身就很慢
    // Where 和 OrderBy 会产生迭代器对象（GC）
}

// ✅ 优化：缓存结果，避免每帧 LINQ
private List<EnemyMovement> aliveEnemies = new List<EnemyMovement>();

void UpdateEnemies()
{
    aliveEnemies.Clear();
    foreach (var enemy in allEnemies)
    {
        if (enemy != null && enemy.currentHealth > 0)
            aliveEnemies.Add(enemy);
    }
}
```

> **原则**：LINQ 适合初始化和低频调用，**不要在 Update / FixedUpdate 中使用 LINQ**。

---

### 2.5 反射

反射是在运行时获取类型信息的能力。

```csharp
using System.Reflection;

// 获取类型
Type type = typeof(EnemyMovement);
Type type2 = enemy.GetType();

// 获取所有 public 字段
FieldInfo[] fields = type.GetFields();
foreach (var field in fields)
{
    Debug.Log($"字段: {field.Name}, 类型: {field.FieldType.Name}");
}

// 通过名称获取字段值
FieldInfo field = type.GetField("maxHealth");
float health = (float)field.GetValue(enemy);

// 通过名称设置字段值
field.SetValue(enemy, 5f);

// 通过字符串调用方法
MethodInfo method = type.GetMethod("TakeDamage");
method.Invoke(enemy, new object[] { 1f });
```

#### 项目中反射的应用场景

```csharp
// Unity Inspector 序列化本质上就是反射
// [SerializeField] 标记的 private 字段通过反射被 Unity 检测并序列化

// UpgradeSystem 中按枚举索引取标题，本质是数组而非反射
titles[(int)type]    // 直接数组索引，比反射快

// 如果用反射实现：
string title = (string)typeof(UpgradeSystem)
    .GetField("titles", BindingFlags.NonPublic | BindingFlags.Static)
    .GetValue(null);
// 功能相同，但性能差几百倍
```

> **性能注意**：反射比直接调用慢 100-1000 倍。在游戏中应缓存反射结果或避免高频使用。Unity 内部在初始化时反射检测一次，之后缓存。

---

## 第三章 Unity C# 实战篇

### 3.1 MonoBehaviour 生命周期

这是 Unity 面试的必考题，也是日常开发的基础。

```
场景加载 / AddComponent
         │
         ▼
    Awake()          ← 第一阶段：实例化，获取组件引用
         │             （此时其他对象可能尚未 Awake）
         ▼
   OnEnable()         ← 每次激活时调用
         │
         ▼
     Start()          ← 第一帧之前，仅一次
         │             （此时所有对象已 Awake，适合组件交互）
         ▼
  ┌─── FixedUpdate() ──← 物理帧（固定时间间隔，默认 0.02s）
  │        │
  │   (等待下一物理帧)
  │        │
  ├─── Update() ───────← 每帧调用
  │        │
  │   LateUpdate() ────← Update 之后，适合摄像机跟随
  │        │
  │   (等待下一帧)
  │        │
  └────────┘
         │
    OnDisable()        ← 每次失活时调用
         │
    OnDestroy()        ← 销毁时调用
```

#### 项目中的生命周期使用

```csharp
// PlayerController.cs — 典型生命周期模式
void Start()
{
    // ✅ Start 中做初始化：获取组件、创建对象池
    Time.timeScale = 1;
    gameOverPanel.SetActive(false);
    rb = GetComponent<Rigidbody>();           // 缓存组件
    animator = GetComponent<Animator>();
    healthBar = GetComponent<HealthBar>();
    expBar = GetComponent<ExpBar>();
    // ...
}

void Update()
{
    // ✅ Update 中处理输入检测
    if (Input.GetMouseButtonDown(0) && !isPaused)
        FireFireball();
}

void FixedUpdate()
{
    // ✅ FixedUpdate 中处理物理移动
    // 因为物理引擎在固定时间步长更新
    Vector3 vel = rb.velocity;
    vel.x = movementX * speed;
    vel.z = movementY * speed;
    rb.velocity = vel;
}
```

#### Awake vs Start 的区别

| | Awake | Start |
|---|---|---|
| 调用时机 | 实例化后立即 | 第一帧之前 |
| 调用次数 | 1 次 | 1 次 |
| 其他对象 | 不保证已初始化 | 所有对象已 Awake |
| 适合做 | 自身组件缓存 | 组件间交互、外部引用 | 

```csharp
// AudioManager.cs — 单例在 Awake 中初始化
void Awake()
{
    if (Instance == null)
        Instance = this;
    else
        Destroy(gameObject);
}

// 如果其他对象的 Start 中调用 AudioManager.Instance
// 必须保证 AudioManager 的 Awake 先执行
// Unity 保证所有 Awake 在所有 Start 之前执行
```

---

### 3.2 序列化

序列化决定了哪些变量会出现在 Inspector 面板中。

```csharp
public class PlayerController : MonoBehaviour
{
    // ✅ public 字段 — 自动序列化，Inspector 可编辑
    public float speed = 10f;
    public GameObject gameOverPanel;

    // ✅ [SerializeField] private — 序列化但不暴露给外部代码
    // Inspector 可编辑，代码中 private
    [SerializeField] private float dashSpeed = 30f;

    // ❌ private — 不序列化，Inspector 不可见
    private Rigidbody rb;
    private bool isPaused = false;

    // ✅ [System.Serializable] — 让自定义类可序列化
    [System.Serializable]
    public class EnemyConfig
    {
        public float health = 2f;
        public float speed = 2.5f;
        public float dropChance = 0.3f;
    }

    public EnemyConfig zombieConfig;    // 自定义类出现在 Inspector 中
}
```

#### 序列化规则速查

| 修饰符 | Inspector 可见 | 序列化 | 代码可访问 |
|---|---|---|---|
| `public` | ✅ | ✅ | ✅ |
| `private` | ❌ | ❌ | ❌ |
| `[SerializeField] private` | ✅ | ✅ | ❌ |
| `[NonSerialized] public` | ❌ | ❌ | ✅ |

#### 项目中的序列化实践

```csharp
// EnemySpawner.cs — 所有需要 Inspector 配置的字段都用 public
public GameObject[] enemyPrefabs;       // 数组也可序列化
public Transform player;
public int maxCount = 30;
public float spawnInterval = 0.5f;

// Fireball.cs — public 字段
public float speed = 20f;
public float lifetime = 3f;

// private 字段不序列化（运行时缓存，不需要 Inspector 编辑）
private float timer;
private ObjectPool pool;
```

---

### 3.3 协程

协程是 Unity 中实现**异步逻辑**的方式，本质是**迭代器 + 状态机**。

#### 3.3.1 基本用法

```csharp
using System.Collections;

// 定义协程
IEnumerator DoSomething()
{
    Debug.Log("开始");
    yield return null;                        // 等待下一帧
    Debug.Log("下一帧");
    yield return new WaitForSeconds(1f);      // 等待 1 秒
    Debug.Log("1秒后");
    yield return new WaitUntil(() => isReady); // 等待条件满足
    Debug.Log("条件满足");
}

// 启动协程
StartCoroutine(DoSomething());

// 停止协程
StopCoroutine(DoSomething());
StopAllCoroutines();
```

#### 3.3.2 协程使用场景

```csharp
// 场景1：延迟执行
IEnumerator DelayedAction(float delay, System.Action action)
{
    yield return new WaitForSeconds(delay);
    action?.Invoke();
}

// 场景2：敌人出生后延迟开始追踪
IEnumerator StartChasing(float delay)
{
    yield return new WaitForSeconds(delay);
    navMeshAgent.SetDestination(player.position);
}

// 场景3：渐变效果
IEnumerator FadeOut(Material mat, float duration)
{
    float elapsed = 0f;
    while (elapsed < duration)
    {
        elapsed += Time.deltaTime;
        float alpha = 1f - elapsed / duration;
        mat.color = new Color(1, 1, 1, alpha);
        yield return null;    // 等待下一帧
    }
    mat.color = new Color(1, 1, 1, 0);
}

// 场景4：敌人生成波次
IEnumerator SpawnWave(int count, float interval)
{
    for (int i = 0; i < count; i++)
    {
        SpawnEnemy();
        yield return new WaitForSeconds(interval);
    }
}
```

#### 3.3.3 协程注意事项

```csharp
// ⚠️ 协程不是线程！
// 协程在主线程执行，只是把逻辑"分帧"了
// yield return null 意味着"本帧到此为止，下一帧继续"

// ⚠️ 协程的生命周期与 MonoBehaviour 绑定
// 如果 GameObject 被销毁，协程自动停止

// ⚠️ WaitForSeconds 受 timeScale 影响
yield return new WaitForSeconds(1f);        // timeScale=0 时永远不返回

// ✅ 使用 WaitForSecondsRealtime 不受 timeScale 影响
yield return new WaitForSecondsRealtime(1f); // 即使暂停也正常等待
```

#### 项目中的协程应用场景

```csharp
// 如果给 PlayerController 加一个受伤无敌帧效果
IEnumerator InvincibilityFrames(float duration)
{
    float elapsed = 0f;
    while (elapsed < duration)
    {
        elapsed += Time.deltaTime;
        // 闪烁效果
        bool visible = Mathf.FloorToInt(elapsed * 10) % 2 == 0;
        GetComponentInChildren<Renderer>().enabled = visible;
        yield return null;
    }
    GetComponentInChildren<Renderer>().enabled = true;
}

// 升级面板出现时的动画
IEnumerator ShowUpgradePanelWithAnimation()
{
    upgradePanel.SetActive(true);
    Vector3 targetScale = Vector3.one;
    upgradePanel.transform.localScale = Vector3.zero;

    while (upgradePanel.transform.localScale.x < 0.99f)
    {
        upgradePanel.transform.localScale = Vector3.Lerp(
            upgradePanel.transform.localScale,
            targetScale,
            0.2f
        );
        yield return null;
    }
    upgradePanel.transform.localScale = targetScale;
}
```

---

### 3.4 ScriptableObject

ScriptableObject 是 Unity 的数据容器，用于创建可复用的数据资产。

#### 3.4.1 与 MonoBehaviour 的区别

| 特性 | MonoBehaviour | ScriptableObject |
|---|---|---|
| 挂载位置 | 场景中的 GameObject | Project 窗口的资产文件 |
| 实例数 | 每个对象一个实例 | 共享一个资产实例 |
| Inspector | 场景 Inspector | 资产 Inspector |
| 内存 | 每实例独立 | 所有引用者共享 |
| 适用场景 | 行为逻辑 | 数据配置 |

#### 3.4.2 创建 ScriptableObject

```csharp
using UnityEngine;

// 定义 ScriptableObject 类
[CreateAssetMenu(fileName = "EnemyConfig", menuName = "Game/Enemy Config")]
public class EnemyConfig : ScriptableObject
{
    public string enemyName = "Zombie";
    public float maxHealth = 2f;
    public float speed = 2.5f;
    public float dropChance = 0.3f;
    public GameObject modelPrefab;

    // 可以包含方法
    public EnemyMovement CreateEnemy(Vector3 position, Transform parent)
    {
        GameObject enemy = Instantiate(modelPrefab, position, Quaternion.identity, parent);
        EnemyMovement movement = enemy.GetComponent<EnemyMovement>();
        movement.maxHealth = maxHealth;
        return movement;
    }
}
```

#### 3.4.3 在项目中使用

```csharp
// 如果用 ScriptableObject 改造 EnemySpawner
public class EnemySpawner : MonoBehaviour
{
    public EnemyConfig[] enemyConfigs;    // Inspector 中拖入配置资产

    void SpawnEnemy(int configIndex)
    {
        EnemyConfig config = enemyConfigs[configIndex];
        EnemyMovement enemy = config.CreateEnemy(spawnPos, enemyGo.transform);
        // 直接使用配置数据
    }
}

// 如果用 ScriptableObject 改造 UpgradeSystem
[CreateAssetMenu(fileName = "UpgradeOption", menuName = "Game/Upgrade Option")]
public class UpgradeOption : ScriptableObject
{
    public string title;
    public string description;
    public Sprite icon;
    public UpgradeType type;
    public float value;
}

public class UpgradeSystem : MonoBehaviour
{
    public UpgradeOption[] allUpgrades;    // Inspector 中配置所有升级选项

    public void ShowUpgrades()
    {
        // 随机选3个
        var shuffled = allUpgrades.ToList();
        // ... 洗牌逻辑 ...
    }
}
```

> **优势**：新增升级类型只需在 Project 窗口右键创建新的 UpgradeOption 资产，不需要改代码。

---

### 3.5 输入系统

#### 项目中使用旧版 Input Manager

```csharp
// PlayerController.cs — 旧版 Input Manager
float movementX = Input.GetAxis("Horizontal");    // A/D, ←/→
float movementY = Input.GetAxis("Vertical");      // W/S, ↑/↓

if (Input.GetMouseButtonDown(0))    // 鼠标左键按下
{
    FireFireball();
}

if (Input.GetKeyDown(KeyCode.Space))    // 空格键按下
{
    Dash();
}

// 射线检测
Ray ray = mainCamera.ScreenPointToRay(Input.mousePosition);
if (Physics.Raycast(ray, out RaycastHit hit, 100f))
{
    // hit.point 是鼠标在3D世界中的位置
}
```

#### Input Manager 常用 API

```csharp
// 按键
Input.GetKey(KeyCode.W)         // 持续按住
Input.GetKeyDown(KeyCode.Space) // 按下瞬间
Input.GetKeyUp(KeyCode.Space)   // 松开瞬间

// 轴（-1 到 1 的浮点值）
Input.GetAxis("Horizontal")     // 平滑过渡
Input.GetAxisRaw("Horizontal")  // 立即变化（-1, 0, 1）

// 鼠标
Input.GetMouseButtonDown(0)     // 左键
Input.GetMouseButtonDown(1)     // 右键
Input.GetMouseButtonDown(2)     // 中键
Input.mousePosition             // 鼠标位置（屏幕坐标）
```

---

## 第四章 性能优化篇

### 4.1 GC（垃圾回收）

GC 是 .NET 运行时自动回收堆内存的机制，是 Unity 性能问题的头号杀手。

#### 4.1.1 什么操作会产生 GC

```csharp
// ❌ 装箱
int x = 10;
object o = x;                        // 堆分配

// ❌ string 拼接
string s = "a" + "b" + "c";          // 每次拼接创建新 string

// ❌ LINQ
enemies.Where(e => e != null).Count();  // 创建迭代器对象

// ❌ lambda 捕获
int captured = 5;
Action action = () => Debug.Log(captured);  // 闭包对象

// ❌ foreach（旧版 Mono 运行时）
foreach (var item in list) { }       // 可能创建迭代器（新版已优化）

// ❌ 创建新数组
Vector3[] array = new Vector3[4] { ... };  // 堆分配

// ❌ GetComponent 返回的数组
Component[] comps = GetComponents<Component>();  // 每次调用分配新数组
```

#### 4.1.2 项目中的 GC 优化实践

```csharp
// ✅ 组件缓存（PlayerController.cs）
// ❌ 每帧 GetComponent
void OnCollisionStay(Collision collision)
{
    var healthBar = GetComponent<HealthBar>();    // 每帧分配
    healthBar.TakeDamage(20f);
}

// ✅ Start 中缓存一次
private HealthBar healthBar;    // 缓存
void Start()
{
    healthBar = GetComponent<HealthBar>();    // 仅一次
}
void OnCollisionStay(Collision collision)
{
    healthBar.TakeDamage(20f);    // 无分配
}

// ✅ 对象池（ObjectPool.cs）
// ❌ 频繁 Instantiate/Destroy（产生大量 GC）
GameObject fireball = Instantiate(fireballPrefab, pos, rot);
// ... 使用后 ...
Destroy(fireball);    // GC 压力

// ✅ 对象池复用
GameObject fireball = fireballPool.Spawn(pos, rot);    // 复用已有对象
// ... 使用后 ...
fireballPool.Despawn(fireball);    // 只是 SetActive(false)，无 GC

// ✅ MaterialPropertyBlock（Footprint.cs）
// ❌ renderer.material 每次创建材质副本
renderer.material.color = new Color(1, 1, 1, alpha);

// ✅ MaterialPropertyBlock 不创建副本
MaterialPropertyBlock block = new MaterialPropertyBlock();
renderer.GetPropertyBlock(block);
block.SetColor("_BaseColor", new Color(1, 1, 1, alpha));
renderer.SetPropertyBlock(block);
```

---

### 4.2 对象池模式

#### 4.2.1 为什么需要对象池

| 方式 | Instantiate + Destroy | 对象池 |
|---|---|---|
| 内存分配 | 每次分配+回收 | 预分配，复用 |
| GC 压力 | 高 | 极低 |
| CPU 开销 | 高（创建/销毁） | 低（激活/失活） |
| 适用场景 | 低频 | 高频（子弹、特效、敌人） |

#### 4.2.2 项目中的对象池实现

```csharp
// ObjectPool.cs — 完整解析
public class ObjectPool : MonoBehaviour
{
    public GameObject prefab;              // 池中对象模板
    public int initialSize = 10;           // 预创建数量
    public Transform parent;               // 父物体

    private Queue<GameObject> pool = new Queue<GameObject>();  // 池（队列）

    void Start()
    {
        // 预创建：提前实例化，失活后放入队列
        for (int i = 0; i < initialSize; i++)
        {
            GameObject obj = Instantiate(prefab, parent);
            obj.SetActive(false);        // 失活
            pool.Enqueue(obj);           // 入队
        }
    }

    public GameObject Spawn(Vector3 position, Quaternion rotation)
    {
        GameObject obj;

        if (pool.Count > 0)
        {
            // 池中有对象：取出复用
            obj = pool.Dequeue();
            obj.transform.position = position;
            obj.transform.rotation = rotation;
        }
        else
        {
            // 池空了：创建新实例（动态扩容）
            obj = Instantiate(prefab, position, rotation, parent);
        }

        obj.SetActive(true);              // 激活

        // 调用接口方法初始化状态
        var pooled = obj.GetComponent<IPooledObject>();
        if (pooled != null)
            pooled.OnSpawn();

        return obj;
    }

    public void Despawn(GameObject obj)
    {
        obj.SetActive(false);             // 失活
        pool.Enqueue(obj);               // 回收到池中
    }
}
```

#### 4.2.3 对象池使用示例

```csharp
// PlayerController.cs — 创建和使用对象池
void Start()
{
    // 创建火球对象池（预创建5个）
    fireballPool = CreatePool(fireballPrefab, skill.transform, 5);
    // 创建脚印对象池（预创建20个）
    footprintPool = CreatePool(footprintPrefab, foot.transform, 20);
}

// 发射火球时从池中取
void FireFireball()
{
    // ...
    fireballPool.Spawn(spawnPos, Quaternion.LookRotation(dir));  // 从池取
}

// 火球命中/超时时回收到池
void Update()
{
    if (timer >= lifetime)
    {
        pool.Despawn(gameObject);    // 回收（不 Destroy）
    }
}
```

---

### 4.3 GetComponent 优化

```csharp
// ❌ 最差：每帧调用
void Update()
{
    Rigidbody rb = GetComponent<Rigidbody>();    // 每帧遍历组件列表
    rb.velocity = ...;
}

// ✅ 好：Start 中缓存
private Rigidbody rb;
void Start()
{
    rb = GetComponent<Rigidbody>();    // 仅一次
}
void Update()
{
    rb.velocity = ...;    // 直接使用缓存
}

// ✅ 最佳：Inspector 序列化引用
public Rigidbody rb;    // Inspector 直接拖入，零运行时开销
// 或
[SerializeField] private Rigidbody rb;

// 优先级：序列化引用 > Start 缓存 > 每帧 GetComponent
```

#### 项目中的 GetComponent 缓存

```csharp
// PlayerController.cs — Start 中一次性获取所有组件
void Start()
{
    rb = GetComponent<Rigidbody>();
    animator = GetComponent<Animator>();
    healthBar = GetComponent<HealthBar>();
    expBar = GetComponent<ExpBar>();
}

// UpgradeSystem.cs — Start 中缓存
void Start()
{
    player = GetComponent<PlayerController>();
    healthBar = GetComponent<HealthBar>();
    magnetDetector = GetComponentInChildren<MagnetDetector>();   // 子物体中查找
}
```

---

### 4.4 避免每帧分配

```csharp
// ❌ 每帧 new
void Update()
{
    Vector3[] corners = new Vector3[4] {    // 堆分配
        new Vector3(0, 0, 0),
        new Vector3(1, 0, 0),
        new Vector3(0, 1, 0),
        new Vector3(1, 1, 0)
    };
}

// ✅ 缓存为类成员
private static readonly Vector3[] viewportCorners = new Vector3[4]
{
    new Vector3(0, 0, 0),
    new Vector3(1, 0, 0),
    new Vector3(0, 1, 0),
    new Vector3(1, 1, 0)
};
// EnemySpawner.cs 就是这么做的
```

---

## 第五章 设计模式篇

### 5.1 单例模式

确保一个类只有一个实例，提供全局访问点。

```csharp
// AudioManager.cs — 项目中的单例实现
public class AudioManager : MonoBehaviour
{
    public static AudioManager Instance { get; private set; }

    void Awake()
    {
        if (Instance == null)
            Instance = this;           // 第一个实例：设为单例
        else
            Destroy(gameObject);       // 后续实例：销毁
    }
}

// 使用
AudioManager.Instance?.PlayPlayerHurt();
// ?. 确保即使 Instance 为 null 也不会报错
```

#### 单例的适用与不适用

```csharp
// ✅ 适合单例：全局唯一的管理器
// — AudioManager（音效管理）
// — GameManager（游戏状态管理）
// — UIManager（UI 管理）

// ❌ 不适合单例：每个实例独立状态的对象
// — PlayerController（可能有多个玩家）
// — EnemyMovement（每个敌人一个）
// — HealthBar（每个角色一个）
```

#### 泛型单例基类

```csharp
// 可以复用的单例基类
public abstract class Singleton<T> : MonoBehaviour where T : MonoBehaviour
{
    public static T Instance { get; private set; }

    protected virtual void Awake()
    {
        if (Instance == null)
        {
            Instance = this as T;
        }
        else
        {
            Destroy(gameObject);
        }
    }

    protected virtual void OnDestroy()
    {
        if (Instance == this)
            Instance = null;
    }
}

// 使用
public class GameManager : Singleton<GameManager>
{
    public GameState currentState;
    // 自动获得单例能力，不需要手写 Awake
}
```

---

### 5.2 观察者模式

当一个对象状态变化时，自动通知所有订阅者。

#### 没有观察者模式的代码（紧耦合）

```csharp
// ❌ ExpBar 直接调用 UpgradeSystem，PlayerController 直接调用 AudioManager
// 类之间直接依赖，改一个类可能影响另一个
public class ExpBar : MonoBehaviour
{
    public void AddExp(float amount)
    {
        // ...
        UpgradeSystem upgradeSystem = GetComponent<UpgradeSystem>();
        if (upgradeSystem != null)
            upgradeSystem.ShowUpgrades();    // 紧耦合
    }
}
```

#### 用观察者模式解耦

```csharp
// ✅ 使用事件解耦
public class ExpBar : MonoBehaviour
{
    public event Action<int> OnLevelUp;    // 事件：升级了

    public void AddExp(float amount)
    {
        // ...
        while (currentExp >= maxExp)
        {
            currentExp -= maxExp;
            level++;
            maxExp += 20f;
        }
        OnLevelUp?.Invoke(level);    // 只通知，不关心谁监听
    }
}

// UpgradeSystem 订阅事件
public class UpgradeSystem : MonoBehaviour
{
    void Start()
    {
        var expBar = GetComponent<ExpBar>();
        expBar.OnLevelUp += HandleLevelUp;    // 订阅
    }

    void HandleLevelUp(int newLevel)
    {
        ShowUpgrades();    // 升级时显示面板
    }

    void OnDestroy()
    {
        var expBar = GetComponent<ExpBar>();
        if (expBar != null)
            expBar.OnLevelUp -= HandleLevelUp;    // 取消订阅
    }
}

// 成就系统也可以订阅同一个事件
public class AchievementSystem : MonoBehaviour
{
    void Start()
    {
        var expBar = player.GetComponent<ExpBar>();
        expBar.OnLevelUp += (level) =>
        {
            if (level >= 5)
                UnlockAchievement("level_5");
        };
    }
}
```

> **核心价值**：ExpBar 不需要知道 UpgradeSystem 和 AchievementSystem 的存在。新增订阅者不需要修改 ExpBar 代码。

---

### 5.3 对象池模式

（已在第四章详细讲解，此处总结设计要点）

```csharp
// 对象池设计要点
// 1. 预创建：Start 中提前实例化
// 2. 复用：Spawn 取出，Despawn 放回
// 3. 动态扩容：池空时自动创建新实例
// 4. 接口回调：IPooledObject.OnSpawn() 重置状态
// 5. 父物体管理：所有池对象挂在统一父物体下
```

---

### 5.4 状态机模式

用状态管理复杂行为逻辑。

```csharp
// 简单状态机实现
public enum GameState
{
    Playing,
    Paused,
    GameOver,
    UpgradeSelection
}

public class GameManager : MonoBehaviour
{
    public GameState currentState = GameState.Playing;

    public void ChangeState(GameState newState)
    {
        currentState = newState;
        switch (currentState)
        {
            case GameState.Playing:
                Time.timeScale = 1;
                break;
            case GameState.Paused:
                Time.timeScale = 0;
                break;
            case GameState.GameOver:
                Time.timeScale = 0;
                ShowGameOverPanel();
                break;
            case GameState.UpgradeSelection:
                Time.timeScale = 0;
                ShowUpgradePanel();
                break;
        }
    }
}

// PlayerController 中的状态管理
private bool isPaused = false;      // 暂停状态
private bool isDashing = false;    // 闪避状态

public void SetPaused(bool paused)
{
    isPaused = paused;    // 外部控制状态
}

void Update()
{
    if (isPaused) return;    // 暂停时跳过输入检测
    // ...
}
```

---

## 第六章 Lua 基础与热更篇

### 6.1 Lua 语法速览

```lua
-- 注释：单行
--[[ 
   注释：多行
--]]

-- 变量（默认全局，加 local 为局部）
local x = 10          -- 局部变量
y = 20                -- 全局变量（避免使用）

-- 数据类型
local num = 3.14      -- number
local str = "hello"   -- string
local flag = true     -- boolean
local nothing = nil   -- nil（空值）
local func = function() end  -- function

-- 表（table）— Lua 唯一的数据结构
local t = {}          -- 空表
t[1] = "first"        -- 数组索引从 1 开始
t.name = "player"     -- 字典形式
t["age"] = 25

-- 遍历表
for k, v in pairs(t) do
    print(k, v)
end

-- 数组遍历
for i, v in ipairs(t) do
    print(i, v)
end

-- 函数
local function add(a, b)
    return a + b
end

-- 条件
if x > 5 then
    print("大")
elseif x > 0 then
    print("小")
else
    print("零或负")
end

-- 循环
for i = 1, 10 do       -- 1 到 10
    print(i)
end

while x > 0 do
    x = x - 1
end
```

### 6.2 Lua 模块

```lua
-- config.lua — 定义模块
local M = {}           -- 创建模块表

M.playerConfig = {
    maxHealth = 100,
    speed = 10,
    fireballCount = 1,
}

M.enemyConfig = {
    { name = "zombie",  health = 2,  speed = 2.5 },
    { name = "fast",    health = 1,  speed = 4.0 },
    { name = "tank",    health = 5,  speed = 1.5 },
}

function M.getEnemyConfig(type)
    return M.enemyConfig[type] or M.enemyConfig[1]
end

return M              -- 返回模块
```

### 6.3 xLua 基本用法

```csharp
// C# 中加载和执行 Lua
using XLua;

LuaEnv luaEnv = new LuaEnv();

// 执行字符串
luaEnv.DoString("print('Hello from Lua!')");

// 执行文件（需要 LuaLoader 配置）
luaEnv.DoString("require 'config'");

// 获取 Lua 全局变量
LuaTable config = luaEnv.Global.Get<LuaTable>("playerConfig");
float maxHealth = config.Get<float>("maxHealth");
float speed = config.Get<float>("speed");

// 调用 Lua 函数
LuaFunction getEnemy = luaEnv.Global.Get<LuaFunction>("getEnemyConfig");
LuaTable enemy = getEnemy.Call(1)[0] as LuaTable;
string name = enemy.Get<string>("name");

// 释放
luaEnv.Dispose();
```

### 6.4 C# 调用 Lua（数据驱动）

```csharp
// 用 Lua 配置替代硬编码
// difficulty_config.lua
/*
local M = {}

M.difficultyCurve = {
    { time = 0,   spawnInterval = 0.5,  maxCount = 30, enemyHp = 2 },
    { time = 30,  spawnInterval = 0.44, maxCount = 36, enemyHp = 5 },
    { time = 60,  spawnInterval = 0.38, maxCount = 42, enemyHp = 8 },
    { time = 90,  spawnInterval = 0.20, maxCount = 60, enemyHp = 10 },
}

function M.getDifficultyAt(elapsedTime)
    local result = M.difficultyCurve[1]
    for _, stage in ipairs(M.difficultyCurve) do
        if elapsedTime >= stage.time then
            result = stage
        end
    end
    return result
end

return M
*/

// C# 中使用
public class DifficultyManager : MonoBehaviour
{
    private LuaEnv luaEnv;
    private LuaTable config;
    private LuaFunction getDifficulty;

    void Start()
    {
        luaEnv = new LuaEnv();
        luaEnv.DoString("require 'difficulty_config'");

        config = luaEnv.Global.Get<LuaTable>("M");    // 获取模块
        getDifficulty = config.Get<LuaFunction>("getDifficultyAt");
    }

    public DifficultyStage GetDifficulty(float elapsedTime)
    {
        object[] results = getDifficulty.Call(elapsedTime);
        LuaTable stage = results[0] as LuaTable;
        return new DifficultyStage
        {
            spawnInterval = stage.Get<float>("spawnInterval"),
            maxCount = stage.Get<int>("maxCount"),
            enemyHp = stage.Get<float>("enemyHp"),
        };
    }

    void OnDestroy()
    {
        config?.Dispose();
        luaEnv?.Dispose();
    }
}
```

### 6.5 热更的本质

```
传统更新流程：
  发现 Bug → 改代码 → 重新编译 → 提交商店审核 → 用户下载新版本
  （iOS 审核需 1-7 天）

Lua 热更流程：
  发现 Bug → 改 Lua 脚本 → 打包为热更包 → CDN 下发 → 客户端启动时下载替换
  （用户无感知，分钟级生效）
```

### 6.6 本项目是否需要 Lua

| 条件 | 本项目 | 结论 |
|---|---|---|
| 平台审核限制 | Windows PC，无审核 | ❌ 不需要 |
| 有服务端 | 无 | ❌ 不需要 |
| 需要持久化 | 无 | ❌ 不需要 |
| 需要动态更新 | 单机游戏 | ❌ 不需要 |

**结论**：当前项目不需要 Lua 热更。配置数据用 JSON 或 ScriptableObject 即可。Lua 热更主要适用于**手游上线运营**场景。

---

## 第七章 面试准备篇

### 7.1 C# 基础必考题

#### Q1: 值类型和引用类型的区别？

> 值类型存储在栈上，赋值时复制值；引用类型存储在堆上，赋值时复制引用。值类型有 int、float、struct、enum；引用类型有 class、string、array。值类型不产生 GC，引用类型的分配和回收会产生 GC。

#### Q2: string 是值类型还是引用类型？

> string 是引用类型，但具有不可变性（immutable）。每次修改 string 都会创建新对象。大量拼接应使用 StringBuilder。

#### Q3: const 和 readonly 的区别？

> `const` 在编译时确定值，不能修改，隐式 static。`readonly` 在运行时（构造函数中）确定值，可以修改一次，可以是实例级。

#### Q4: abstract class 和 interface 的区别？

> 抽象类可以包含实现代码、字段、构造函数；接口只包含方法/属性签名（C# 8.0 前不能有实现）。类只能单继承抽象类，但可以实现多个接口。

#### Q5: 以下代码哪里有装箱？

```csharp
int x = 10;
object o = x;          // ← 装箱
ArrayList list = new ArrayList();
list.Add(1);           // ← 装箱
string s = x.ToString(); // ← 装箱（int.ToString 内部可能装箱）
```

### 7.2 Unity 必考题

#### Q6: MonoBehaviour 生命周期顺序？

> Awake → OnEnable → Start → FixedUpdate → Update → LateUpdate → OnDisable → OnDestroy。Awake 获取组件，Start 做组件间交互，FixedUpdate 处理物理，Update 处理输入，LateUpdate 处理摄像机跟随。

#### Q7: Update、FixedUpdate、LateUpdate 的区别？

> - Update：每帧调用，时间间隔不固定
> - FixedUpdate：固定时间间隔（默认 0.02s），适合物理计算（Rigidbody）
> - LateUpdate：所有 Update 之后调用，适合摄像机跟随（避免抖动）

#### Q8: 协程的原理？是线程吗？

> 协程不是线程，在主线程执行。底层是迭代器（IEnumerator）+ 状态机。`yield return` 将控制权交还 Unity，下一帧或条件满足时恢复执行。协程生命周期绑定 MonoBehaviour，GameObject 销毁时自动停止。

#### Q9: 如何减少 GC？

> 1. 缓存组件引用（GetComponent 在 Start 中调用）
> 2. 使用对象池替代频繁 Instantiate/Destroy
> 3. 避免 string 拼接，用 StringBuilder
> 4. 避免 LINQ 在 Update 中使用
> 5. 使用 MaterialPropertyBlock 替代 renderer.material
> 6. 用 List\<T> 替代 ArrayList（避免装箱）

#### Q10: Instantiate/Destroy 为什么慢？

> 1. 堆内存分配（产生 GC）
> 2. Unity 内部资产引用计数更新
> 3. 可能触发 GC.Collect
> 4. 频繁使用导致内存碎片
> 解决方案：对象池（预创建 + 复用）

### 7.3 设计模式必考题

#### Q11: 实现一个泛型单例基类

```csharp
public abstract class Singleton<T> : MonoBehaviour where T : MonoBehaviour
{
    public static T Instance { get; private set; }
    protected virtual void Awake()
    {
        if (Instance == null) { Instance = this as T; }
        else { Destroy(gameObject); }
    }
    protected virtual void OnDestroy()
    {
        if (Instance == this) Instance = null;
    }
}
```

#### Q12: 实现一个简单对象池

```csharp
public class ObjectPool<T> where T : new()
{
    private Queue<T> pool = new Queue<T>();
    private Func<T> factory;

    public ObjectPool(Func<T> factory, int initialSize = 10)
    {
        this.factory = factory;
        for (int i = 0; i < initialSize; i++)
            pool.Enqueue(factory());
    }

    public T Get() => pool.Count > 0 ? pool.Dequeue() : factory();
    public void Return(T item) => pool.Enqueue(item);
}
```

### 7.4 高频手写代码题

#### Q13: 反转字符串

```csharp
public string Reverse(string s)
{
    if (string.IsNullOrEmpty(s)) return s;
    char[] chars = s.ToCharArray();
    int left = 0, right = chars.Length - 1;
    while (left < right)
    {
        (chars[left], chars[right]) = (chars[right], chars[left]);
        left++; right--;
    }
    return new string(chars);
}
```

#### Q14: 二分查找

```csharp
public int BinarySearch(int[] arr, int target)
{
    int left = 0, right = arr.Length - 1;
    while (left <= right)
    {
        int mid = left + (right - left) / 2;
        if (arr[mid] == target) return mid;
        if (arr[mid] < target) left = mid + 1;
        else right = mid - 1;
    }
    return -1;
}
```

#### Q15: 冒泡排序

```csharp
public void BubbleSort(int[] arr)
{
    for (int i = 0; i < arr.Length - 1; i++)
    {
        for (int j = 0; j < arr.Length - 1 - i; j++)
        {
            if (arr[j] > arr[j + 1])
            {
                int temp = arr[j];
                arr[j] = arr[j + 1];
                arr[j + 1] = temp;
            }
        }
    }
}
```

---

## 第八章 实践任务篇

> 以下任务基于迷你游戏项目代码，建议按顺序完成。

### 任务1：理解值类型与引用类型

**目标**：理解 struct 和 class 在赋值时的不同行为。

**步骤**：

1. 打开 `EnemySpawner.cs`，找到 `List<GameObject> enemies`
2. 思考：`enemies.RemoveAll(e => e == null)` 中的 `e` 是值类型还是引用类型？
3. 写一个测试：在 `Start()` 中添加以下代码并运行观察：

```csharp
void Start()
{
    // 测试1：值类型
    Vector3 a = new Vector3(1, 2, 3);
    Vector3 b = a;
    b.x = 99;
    Debug.Log($"值类型: a.x={a.x}, b.x={b.x}");    // a.x=1, b.x=99

    // 测试2：引用类型
    List<int> list1 = new List<int> { 1, 2, 3 };
    List<int> list2 = list1;
    list2[0] = 99;
    Debug.Log($"引用类型: list1[0]={list1[0]}, list2[0]={list2[0]}");    // 都是 99
}
```

**预期结果**：理解值类型复制值，引用类型复制引用。

---

### 任务2：给 HealthBar 添加事件通知

**目标**：实践观察者模式，解耦血量变化的通知。

**当前问题**：`PlayerController.OnCollisionStay` 中直接调用 `healthBar.TakeDamage`，如果未来要添加受伤特效、成就追踪等，都要修改 PlayerController。

**步骤**：

1. 打开 `HealthBar.cs`
2. 添加事件：

```csharp
public event Action<float> OnHealthChanged;    // 血量变化事件
public event Action OnDeath;                     // 死亡事件

public void TakeDamage(float damage)
{
    currentHealth = Mathf.Max(0f, currentHealth - damage);
    UpdateFill();
    OnHealthChanged?.Invoke(currentHealth);      // 通知

    if (currentHealth <= 0f)
        OnDeath?.Invoke();                       // 通知死亡
}
```

3. 修改 `PlayerController.OnCollisionStay`，在 Start 中订阅事件：

```csharp
void Start()
{
    // ... 原有代码 ...
    healthBar.OnDeath += HandlePlayerDeath;      // 订阅死亡事件
}

void HandlePlayerDeath()
{
    AudioManager.Instance?.PlayPlayerDeath();
    ShowGameOver();
    Destroy(gameObject);
}

void OnDestroy()
{
    if (healthBar != null)
        healthBar.OnDeath -= HandlePlayerDeath;  // 取消订阅
}
```

**验证**：运行游戏，让敌人接触玩家，血量归零时仍然触发死亡逻辑。

---

### 任务3：用 ScriptableObject 改造升级系统

**目标**：将 UpgradeSystem 的硬编码升级选项改为数据驱动。

**步骤**：

1. 创建新脚本 `UpgradeOption.cs`：

```csharp
using UnityEngine;

[CreateAssetMenu(fileName = "UpgradeOption", menuName = "Game/Upgrade Option")]
public class UpgradeOption : ScriptableObject
{
    public UpgradeSystem.UpgradeType type;
    public string title;
    public string description;
    public Sprite icon;
    public float value = 1f;
}
```

2. 修改 `UpgradeSystem.cs`：

```csharp
public class UpgradeSystem : MonoBehaviour
{
    public UpgradeOption[] allUpgrades;    // Inspector 中拖入配置
    public GameObject upgradePanel;
    public UpgradeCard[] cards;

    // 删除原来的 titles/descs/icons 硬编码数组
    // 保留 ShowUpgrades 和 SelectUpgrade 的核心逻辑

    public void ShowUpgrades()
    {
        // 随机选3个
        List<UpgradeOption> shuffled = new List<UpgradeOption>(allUpgrades);
        for (int i = shuffled.Count - 1; i > 0; i--)
        {
            int j = Random.Range(0, i + 1);
            (shuffled[i], shuffled[j]) = (shuffled[j], shuffled[i]);
        }

        for (int i = 0; i < cards.Length && i < 3; i++)
        {
            UpgradeOption option = shuffled[i];
            cards[i].SetData(option.type, option.title, option.description);
            if (option.icon != null && cards[i].icon != null)
                cards[i].icon.sprite = option.icon;
            cards[i].SetupCallback(this);
        }

        if (player != null) player.SetPaused(true);
        Time.timeScale = 0;
        upgradePanel.SetActive(true);
    }
}
```

3. 在 Project 窗口右键创建 4 个 UpgradeOption 资产文件
4. 在 Inspector 中配置每个升级的标题、描述、图标、数值

**验证**：运行游戏，升级面板正常显示，点击选项效果正常。

---

### 任务4：给敌人添加受伤闪烁效果

**目标**：实践协程和 MaterialPropertyBlock。

**步骤**：

1. 打开 `EnemyMovement.cs`
2. 添加受伤闪烁协程：

```csharp
private MaterialPropertyBlock damageBlock;
private Renderer enemyRenderer;

void Start()
{
    // ... 原有代码 ...
    enemyRenderer = GetComponentInChildren<Renderer>();
    damageBlock = new MaterialPropertyBlock();
}

public void TakeDamage(float damage)
{
    currentHealth -= damage;
    // ... 原有代码 ...

    // 开始受伤闪烁
    StartCoroutine(DamageFlash());
}

IEnumerator DamageFlash()
{
    if (enemyRenderer == null) yield break;

    float elapsed = 0f;
    float duration = 0.2f;

    while (elapsed < duration)
    {
        elapsed += Time.deltaTime;
        float t = elapsed / duration;

        // 红色闪烁
        Color flashColor = Color.Lerp(Color.red, Color.white, t);
        enemyRenderer.GetPropertyBlock(damageBlock);
        damageBlock.SetColor("_BaseColor", flashColor);
        enemyRenderer.SetPropertyBlock(damageBlock);

        yield return null;
    }

    // 恢复原色
    enemyRenderer.GetPropertyBlock(damageBlock);
    damageBlock.SetColor("_BaseColor", Color.white);
    enemyRenderer.SetPropertyBlock(damageBlock);
}
```

**验证**：用火球攻击敌人，敌人受伤时红色闪烁 0.2 秒。

---

### 任务5：实现简易计时器工具类

**目标**：练习泛型方法、委托、struct。

**步骤**：

1. 创建新脚本 `Timer.cs`：

```csharp
using System;
using UnityEngine;

public class Timer
{
    private float duration;
    private float elapsed;
    private Action onComplete;
    private bool isRunning;

    public bool IsRunning => isRunning;

    public void Start(float duration, Action onComplete)
    {
        this.duration = duration;
        this.elapsed = 0f;
        this.onComplete = onComplete;
        this.isRunning = true;
    }

    public void Tick(float deltaTime)
    {
        if (!isRunning) return;

        elapsed += deltaTime;
        if (elapsed >= duration)
        {
            isRunning = false;
            onComplete?.Invoke();
        }
    }

    public void Cancel()
    {
        isRunning = false;
    }

    public float Progress => isRunning ? Mathf.Clamp01(elapsed / duration) : 0f;
}
```

2. 在 `EnemySpawner.cs` 中使用：

```csharp
private Timer bossTimer2;

void Start()
{
    bossTimer2 = new Timer();
    bossTimer2.Start(10f, () =>
    {
        SpawnBoss();
        bossTimer2.Start(10f, () => SpawnBoss());    // 循环
    });
}

void Update()
{
    bossTimer2.Tick(Time.deltaTime);
}
```

**验证**：Boss 每 10 秒生成一次，功能与原来一致。

---

### 任务6：分析项目中的 GC 点

**目标**：找出项目中可能产生 GC 的代码。

**检查清单**：

1. `EnemySpawner.cs` 的 `enemies.RemoveAll(e => e == null)` — 每次调用产生迭代器
2. `EnemyMovement.cs` 的 `GameObject.Find("PickUp")` — 在 `Die()` 中调用，频率不高但可用缓存优化
3. `EnemySpawner.cs` 的 `FormatTime` — 使用 `string.Format`，每帧调用产生字符串
4. `PlayerController.cs` 的 `new Color(...)` — 每帧在冷却 UI 更新中创建 Color struct（值类型，无 GC）

**优化练习**：

```csharp
// 优化1: FormatTime 改用 StringBuilder
private System.Text.StringBuilder timeBuilder = new System.Text.StringBuilder();

string FormatTime(float time)
{
    int minutes = Mathf.FloorToInt(time / 60f);
    int seconds = Mathf.FloorToInt(time % 60f);
    timeBuilder.Clear();
    timeBuilder.Append(minutes.ToString("00"));
    timeBuilder.Append(":");
    timeBuilder.Append(seconds.ToString("00"));
    return timeBuilder.ToString();
}

// 优化2: Die() 中的 Find 缓存
private static Transform pickUpParent;

void Die()
{
    if (pickUpParent == null)
        pickUpParent = GameObject.Find("PickUp")?.transform;
    // 使用 pickUpParent
}
```

---

### 任务7：用 JSON 替代硬编码配置

**目标**：不引入 Lua，仅用 Unity 内置 JSON 实现数据驱动配置。

**步骤**：

1. 创建 JSON 配置文件 `Assets/Config/difficulty.json`：

```json
{
    "stages": [
        { "time": 0,   "spawnInterval": 0.5,  "maxCount": 30, "enemyHp": 2 },
        { "time": 30,  "spawnInterval": 0.44, "maxCount": 36, "enemyHp": 5 },
        { "time": 60,  "spawnInterval": 0.38, "maxCount": 42, "enemyHp": 8 },
        { "time": 90,  "spawnInterval": 0.20, "maxCount": 60, "enemyHp": 10 }
    ]
}
```

2. 创建数据类：

```csharp
[System.Serializable]
public class DifficultyConfig
{
    public DifficultyStage[] stages;
}

[System.Serializable]
public class DifficultyStage
{
    public float time;
    public float spawnInterval;
    public int maxCount;
    public float enemyHp;
}
```

3. 加载和使用：

```csharp
using UnityEngine;

public class DifficultyLoader : MonoBehaviour
{
    private DifficultyConfig config;

    void Start()
    {
        TextAsset jsonFile = Resources.Load<TextAsset>("Config/difficulty");
        config = JsonUtility.FromJson<DifficultyConfig>(jsonFile.text);
    }

    public DifficultyStage GetDifficultyAt(float elapsedTime)
    {
        DifficultyStage result = config.stages[0];
        foreach (var stage in config.stages)
        {
            if (elapsedTime >= stage.time)
                result = stage;
        }
        return result;
    }
}
```

**验证**：修改 JSON 文件后重新运行，难度参数随之变化。

---

### 学习路线总结

```
第一阶段：C# 基础（1-2 周）
├── 变量与类型（值类型 vs 引用类型）
├── 控制流（if / for / while / switch）
├── 类与对象（字段、属性、方法）
├── 继承与多态（virtual / override / abstract）
└── 集合（List / Queue / Dictionary）

第二阶段：C# 进阶（1-2 周）
├── 泛型（List<T> / GetComponent<T>）
├── 委托与事件（Action / event）
├── Lambda 与闭包
├── LINQ（Where / Select / OrderBy）
└── 反射基础

第三阶段：Unity 实战（2-3 周）
├── MonoBehaviour 生命周期
├── 序列化与 Inspector
├── 协程（IEnumerator / yield return）
├── ScriptableObject 数据驱动
└── 输入系统

第四阶段：性能优化（1 周）
├── GC 原理与避免
├── 对象池模式
├── GetComponent 缓存
└── MaterialPropertyBlock

第五阶段：设计模式（1 周）
├── 单例模式
├── 观察者模式
├── 对象池模式
└── 状态机模式

第六阶段：Lua 热更（了解，1 周）
├── Lua 语法基础
├── xLua 集成
├── C# ↔ Lua 交互
└── 热更原理

第七阶段：面试准备（1 周）
├── C# 基础题
├── Unity 生命周期题
├── 手写代码题
└── 性能优化题
```

---

## 参考资源

| 资源 | 链接 | 说明 |
|---|---|---|
| Microsoft Learn (C#) | https://learn.microsoft.com/dotnet/csharp/ | C# 官方教程 |
| Unity Learn | https://learn.unity.com | Unity 官方教学 |
| Unity 脚本 API | https://docs.unity3d.com/ScriptReference/ | Unity API 参考 |
| Lua 官方手册 | https://www.lua.org/manual/5.4/ | Lua 语言参考 |
| xLua GitHub | https://github.com/Tencent/xLua | 腾讯开源 Lua 框架 |
