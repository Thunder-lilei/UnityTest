

## Codely Structured Memories

### User
- [2026-08-12 16:18:23] 用户偏好：不动手只教教程时给清晰步骤即可；操作类任务直接执行不需要反复确认。用户是 Unity 学习者，正在通过做一个迷你游戏项目学习 Unity 开发。
### Feedback
- [2026-08-12 16:19:09] FBX 动画导入设置 lastFrame 必须用帧数（秒 × frameRate），不能直接用秒数。之前误将 lastFrame 设为秒值（如 1.667）导致所有动画片段被截断到 ~0.056s。Quaternius 模型 frameRate=30fps。**Why:** lastFrame 在 ModelImporterClipAnimation 中是帧号而非时间。**How to apply:** 通过脚本批量设置 FBX clip loopTime 时，lastFrame = clip.length * clip.frameRate。
- [2026-08-12 17:05:21] Prefab Variant 的 SkinnedMeshRenderer.bones[] 数组在编辑器中显示正确但运行时实例化后全部变为 NULL（序列化缺陷）。**Why:** EnemyFast 是 FBX 的 Prefab Variant，bones[] 引用 FBX 内部 Transform，CopyAsset 或 LoadPrefabContents→SaveAsPrefabAsset 均会破坏引用链，导致运行时 bones 全为 NULL → bounds 为零 → 视锥剔除使怪物不可见（但有碰撞体仍能攻击/被命中）。**How to apply:** 不能通过复制或重建 Variant Prefab 来修复。最终方案：Spawner 的 enemyPrefabs[1] 直接引用原始模型 prefab（与 [0] 相同），运行时按 typeIndex 设置差异属性（scale/speed）。不再使用独立的 EnemyFast.prefab。
- [2026-08-13 14:53:45] Humanoid 动画模型必须通过 PrefabUtility.InstantiatePrefab 加载到场景中，不能用 Object.Instantiate 克隆 FBX。**Why:** Object.Instantiate 会破坏 Avatar 的骨骼映射链（GetBoneTransform 返回 NULL），导致角色运行时显示 T-pose。PrefabUtility.InstantiatePrefab 保留 FBX Prefab 引用链，Avatar 映射正常。**How to apply:** 需要将 FBX 模型放入场景时，始终用 PrefabUtility.InstantiatePrefab；PlayerController 中 Animator 获取改为 GetComponentInChildren<Animator>()（Animator 在模型子对象上）。
- [2026-08-13 15:13:01] 运行时反射修改的 private 字段值（如 HealthBar.maxHealth/currentHealth）会被 Unity 序列化到场景文件中，退出 Play Mode 后仍保留。**Why:** 之前用反射设 maxHealth=99999 测试，结果场景保存了该值，且 currentHealth=0 被保存导致后续进游戏秒死触发 Time.timeScale=0 冻结动画。**How to apply:** 不要在运行时通过反射修改序列化字段做测试；如需提升血量测试，应在 Play Mode 前编辑场景或在代码中临时修改默认值，测试后恢复。
- [2026-08-13 21:21:52] 凡是涉及脚本改动或较大规模变更，完成后必须运行测试用例验证无回归。如有新功能需要及时补充对应测试用例。不需要用户提醒，主动执行。**Why:** 用户明确要求将此视为项目规则。**How to apply:** 每次脚本改动/架构变更后，提示用户运行 Test Runner（脚本无法自动触发），并根据改动补充新测试用例。
- [2026-08-13 21:43:54] Quaternius/KayKit 的 FBX 模型导出时自带 X=270° 旋转（Blender Z-up→Y-up 转换），实例化时必须保留在子对象的 localRotation 中。Kenney 模型无此旋转。摆放时需逐个检查 bounds 的 Y 维度是否为最大值（判断是否直立）以及 min.y 是否≈0（判断是否贴地），不满足则修正旋转和 Y 坐标。**Why:** 用户发现建筑模型倒下了。**How to apply:** 每次将第三方 FBX 放入场景后，必须验证朝向和地面贴合。
- [2026-08-13 21:43:58] 资源存放位置必须严格按照项目目录结构规范。第三方资产按来源分目录（Assets/Quaternius、Assets/Kenney、Assets/Kaykit），下载缓存在 Assets/TJGenerators/DownloadedAssets/。如遇规范不明确的情况，必须先跟用户确认，不能随意放置。**Why:** 用户明确指出随意放置是错误行为。**How to apply:** 下载/导入新资源时，先检查设计文档第 11 节目录结构总览，确认归属目录后再放置。
- [2026-08-17 16:02:53] TMP 默认字体（LiberationSans SDF）不支持 Emoji/Unicode 符号渲染（❤⚡🔥等会显示为空白或水印）。**Why:** SDF 字体只包含 ASCII 字符集，Emoji 需要专门的 Emoji 字体资产。**How to apply:** 游戏内 UI 文字图标用纯 ASCII 文本标签（如 HP/SPD/FIRE/DMG）替代 Emoji，或安装 Emoji TMP 字体资产。
- [2026-08-17 16:26:16] Unity Test Framework 1.1.33 的 TestRunnerApi 在 Tuanjie 引擎中可通过菜单项 Tools/Run All Tests 触发（RunTestsEditor.cs 放在 Assets/Tests/ 下，Tests.asmdef 需添加 UnityEditor.TestRunner + UnityEngine.TestRunner 引用）。batchmode -runTests 方式因编辑器占用项目而失败，改用编辑器内 API 运行。**Why:** Tuanjie 编辑器已占用项目时 batchmode 无法启动第二个实例。**How to apply:** 在编辑器内通过 TestRunnerApi.Execute() + IErrorCallbacks 回调运行测试，15s 后读取 Console 获取结果。
- [2026-08-17 16:26:19] EditMode 测试 MonoBehaviour 子类时，Start() 不会自动调用，private 字段（如 rb/animator）为 null。**Why:** EditMode 不执行 Unity 生命周期。**How to apply:** 在 [SetUp] 中通过反射手动初始化 private 字段，或改用 [UnityTest] + [UnitySetUp]（需 PlayMode asmdef）。
- [2026-08-18 16:44:59] 从特效库下载的 ParticleSystem 材质默认使用内置管线 shader（Particles/Standard Unlit），在 URP 项目中会导致原生层崩溃（编辑器直接关闭，无 C# 异常）。**Why:** 内置管线 shader 在 URP 下渲染 ParticleSystemRenderer 时触发 native crash。**How to apply:** 每次从特效库下载 ParticleSystem 特效后，必须检查材质 shader 并替换为 URP 对应版本（Universal Render Pipeline/Particles/Unlit），否则运行时 Instantiate 触发崩溃。
- [2026-08-19 20:30:04] Tuanjie/Unity URP 项目中材质 shader 选择：不能用 Hidden/Internal-Colored（Hidden shader 不会被渲染管线渲染），不能用 Sprites/Default（在 URP 中可能不渲染 3D Mesh）。必须用 Universal Render Pipeline/Unlit 或 Universal Render Pipeline/Lit。**Why:** ShockwaveMat 之前用了 Hidden/Internal-Colored 导致冲击波圆环完全不可见。**How to apply:** 创建 URP 项目材质时，始终用 URP 系列 shader，避免 Hidden/和 Sprites/Default。
- [2026-08-19 20:30:04] 平铺在地面上的自定义 Mesh（Y 轴厚度为 0）在 45° 俯视摄像机下可能被 Unity 视锥剔除判定不可见。**Why:** 圆环 Mesh bounds Y=0 导致 isVisible=False。**How to apply:** 给平铺 Mesh 的顶点添加少量 Y 偏移（如 0.1）使 bounds 有 Y 厚度；同时检查三角形绕序确保法线朝上（面向摄像机）。
- [2026-08-19 20:33:23] 当 Shader.Find 找不到指定 shader 时 fallback 到了 Hidden shader，导致材质不渲染。**Why:** 用户要求类似情况必须先确认再执行，不能擅自 fallback。**How to apply:** 代码中 Shader.Find 返回 null 或 fallback 到 Hidden/ 开头的 shader 时，必须暂停并与用户确认替代方案，不能自行决定使用 Hidden shader。
- [2026-08-19 20:33:23] 所有新增或调整的视觉相关内容（特效、动画、材质、模型等）交付前必须自行截图确认运行时表现正常再交给用户验收。**Why:** 用户要求避免反复返工，之前冲击波特效多次交付后才发现不可见。**How to apply:** 每次完成视觉相关改动后，进入 Play 模式截图验证效果确实可见且符合预期，确认无误后再通知用户验收。
- [2026-08-19 20:51:17] 敌人使用 NavMeshAgent 移动，通常没有 Rigidbody 组件。需要对敌人施加物理力（击退/爆炸等）时，不能用 AddForce，应通过临时禁用 NavMeshAgent + Transform.Lerp 位移实现。**Why:** ShockwaveEffect 原用 AddForce 击退敌人，但敌人无 Rigidbody 导致击退无效。**How to apply:** 对 NavMeshAgent 敌人施加位移效果时，禁用 Agent → Lerp 移动 → SamplePosition 确保落点 → 重新启用。

### Project
- [2026-08-12 16:18:23] 项目从 C:\AIProject\ 搬迁到 C:\Learn\，manifest.json 中 cn.tuanjie.ai.generators 的 file 引用路径需同步更新，否则控制台报包依赖错误。已于 2026-08-12 修复。
- [2026-08-13 15:13:03] 项目美术风格已确认并写入设计文档.md 6.3 节：低多边形卡通风格（Low-poly Stylized）。选材原则：优先 Kenney/KayKit/Quaternius 的 CC0 低多边形资产，排除 Polyhaven 写实素材。

- [2026-08-12 21:38:22] 火球特效已从原版 FireBall.vfx 替换为增强版 FireBallEnhanced.vfx（自定义火焰贴图 FireParticle.png，80粒子/秒，5段颜色渐变，尺寸先膨胀后收缩曲线）。FireBall.prefab 的 VisualEffect 引用已更新。旧版 FireBall.vfx 已于 2026-08-12 资产清理时删除。

- [2026-08-12 17:06:49] 场景自然素材已重新布局：138 个实例统一放在 NatureDecoration 父对象下。布局策略：围墙外半径 12-30 密集森林环带（46棵树）+ 35-60 远景稀疏树林（22棵）+ 14-45 散布岩石（18个）+ 13-35 灌木藤蔓（16个）+ 26-55 地形装饰（10个山丘/自然地块）+ 围墙内侧边缘装饰（10个）+ 四面围墙栅栏（16个）。Ground 320×320 大平面，材质为纯色草绿 URP Lit（Assets/Materials/GroundGrass.mat）。


- [2026-08-12 16:19:01] 怪物动画系统已修复：EnemyMovement.cs 在 Update() 中通过 animator.SetFloat("Speed", navMeshAgent.velocity.magnitude) 驱动动画。三个 Animator Controller（basic/chubby/ribcage）均添加了 Speed(float) 参数和 Idle↔Crawl 过渡条件（Speed>0.1 进入 Crawl，<0.1 回到 Idle）。FBX 动画循环已在 .meta clipAnimations 中设置 loopTime=true（Crawl/Idle/Walk/Run）。
- [2026-08-12 16:19:05] EnemyTank 和 EnemyBoss 模型已从旧版 zombie_arm（无 Crawl 动画）替换为 Quaternius Zombie Apocalypse Kit 同系列模型：Tank→Zombie_Chubby，Boss→Zombie_Ribcage。旧 zombie_arm FBX/controller/prefab 已删除。关键：替换模型时必须将子对象直接展开到 Prefab root 下（Instantiate 后 reparent），不能嵌套整个 model prefab，否则动画 clip 骨骼路径不匹配导致动画不播放。
- [2026-08-12 17:05:25] 火球 bug 已修复：FireBall.OnTriggerEnter 原来在 if(Enemy) 块外执行 PlayFireballHit + Despawn，导致火球碰到 MagnetDetector（玩家子物体，半径3m触发器）即被回收。修复：忽略非敌人的触发器（isTrigger && !Enemy）和 Player 层。另外 FireBall.OnSpawn() 增加 vfx.Reinit()+Play() 修复对象池复用时 VFX 不重启问题。所有敌人 SMR 设为 updateWhenOffscreen=True。隐形怪物 bug 根因有二：①场景中残留一个 EnemyFast(Clone) 对象（bones[] 全 NULL），已删除；②EnemyFast.prefab 的 Variant bones[] 运行时丢失，已改为 Spawner 直接引用原始模型 prefab + 运行时设置差异属性。
- [2026-08-13 14:54:05] 场景中曾存在两个同名 Player 根对象（一个有 PlayerController 无模型，一个有 Y Bot 模型无 PlayerController），导致：①火球双发（两个 PlayerController 同时响应鼠标点击）；②角色环绕初始位置运动（PlayerController 操作空对象 Rigidbody，模型不受控）。已合并为单个 Player 对象，YBot 作为子对象（Animator 在 YBot 上）。另外 GameOverPanel 曾残留在激活状态 + currentHealth=0 导致进游戏秒死，已修复。
- [2026-08-13 15:12:56] 主角模型已替换为标准 Mixamo Y Bot（灰色人偶）。资源从 C:\Learn\UnityTest\Wuxia\Assets\Model\Characters 复制到 Assets/Models/YBot/（Y Bot.fbx + Idle/Walking/Running/Jumping.fbx）。Animator Controller 为新建的 Assets/Animations/PlayerController.controller（Idle↔Walk↔Run，Speed 参数驱动）。PlayerController.cs 的 Start() 中 Animator 获取改为 GetComponentInChildren<Animator>()。YBot 通过 PrefabUtility.InstantiatePrefab 加载为 Player 子对象，Animator 在 YBot 上。
- [2026-08-13 16:04:56] 敌人死亡溶解特效已完成：手写 URP HLSL Shader（Dissolve.shader，ShaderLab+HLSL，AlphaToMask+噪声+边缘发光）+ DissolveMat.mat + DissolveEffect.cs 脚本（运行时替换材质+动画DissolveAmount 0→1）。三个敌人 Prefab（Zombie_Basic/EnemyTank/EnemyBoss）已添加 DissolveEffect 组件并关联材质。EnemyMovement.cs Die() 方法已集成溶解逻辑。教程文档：溶解特效制作教程.md。
- [2026-08-14 16:45:58] v1.4 架构重构完成：①PlayerController 拆分为 4 个组件（PlayerMovement/PlayerCombat/PlayerHealth/PlayerInteraction），用 C# Action 事件解耦（OnDashStateChanged/OnPlayerDied）②ScriptableObject 敌人配置（EnemyData.cs + 4 个 .asset 文件）③20 个脚本添加命名空间（Game.Player/Enemy/Combat/Audio/UI/Systems）+ Game.asmdef 程序集定义 ④5 个 EditMode 测试文件（24 个用例）+ 测试文档 ⑤材质命名统一（wall→Wall, Dynamic Obstacle→DynamicObstacle, FootprintMat→Footprint, DissolveMat→Dissolve）

- [2026-08-13 19:56:26] URP Bloom 后处理不生效问题：URPRenderer.asset 中 postProcessData={fileID:0}（为空），导致后处理 Pass 未启用。Volume + Profile 配置正确（Bloom intensity=3, threshold=0.5, Vignette intensity=0.4），但渲染器未执行后处理。待解决。
- [2026-08-13 21:43:51] 场景装饰素材已补充：9 个低多边形素材放入场景，统一挂在 SceneProps 父对象下。建筑类 3 个（Quaternius Fantasy RTS 中世纪民居/东亚小屋/木质哨所，scale=150，围墙外背景）；道具类 6 个（篝火 scale=200、蜡烛 scale=100、木栅栏门 scale=2、铁栅栏 scale=1.5、损坏栅栏 scale=1.5、运石车 scale=1.5，围墙内布置）。设计文档第 11 节目录结构总览已同步更新。
- [2026-08-14 17:01:19] 近战斩击技能已完成（2026-08-14）：MeleeCombat.cs（自动定时半圆范围攻击，间隔2s/伤害2/半径4m，Dot检测前方半圆，Instantiate+Destroy 非对象池）+ SlashEffect.cs（ParticleSystem 定时销毁，非对象池）+ SlashEffect.prefab（基于特效库 SwordSlashThickBlue，原始粒子参数保留）。特效资源已迁移到 Assets/Effects/Slash/（slash03_AB.mat/slash03_ADD.mat/slash03.png/ETFX_CirclePlane2.FBX），ETFGRotation.cs 在 Assets/Scripts/。TJGeneratorLibEffects 目录已删除。升级系统已集成 IncreaseMeleeDamage/ReduceSlashInterval。AudioManager 新增 PlaySlashAttack()。修复 LuaManager.cs 两个 XLua API 错误（AddLoader 返回 byte[]、GetLuaIterator→手动遍历）。

- [2026-08-14 16:46:18] UpgradeSystem 已被外部重写为 Lua 驱动版（2026-08-14）：升级数据从 Assets/Lua/upgrades.lua.txt 加载，C# 只负责执行。UpgradeCard 不再依赖 UpgradeType 枚举，改为 string action + float value。ApplyAction switch 分发 action 字符串到对应 C# 方法。新增升级只需改 Lua 文件 + ApplyAction 加 case。LuaManager.cs（单例）管理 LuaEnv 生命周期。注意 LuaManager 有 1 个 obsolete warning（LuaTable.Get<T> 建议用 Get<TKey,TValue>），不影响运行。
- [2026-08-14 16:49:19] Lua 热更升级系统已集成（2026-08-14）：xLua 框架安装到 Assets/XLua/（3 个 asmdef：XLuaRuntime/XLua.Editor/XLua.EditorConfig）+ Assets/Plugins/x86_64/xlua.dll。LuaManager.cs（单例）管理 LuaEnv 生命周期，自定义 loader 从 Assets/Lua/ 加载 .lua.txt 文件。UpgradeSystem.cs 改为 Lua 驱动：从 upgrades.lua.txt 读取升级数据（7 个升级选项），ApplyAction switch 分发 action 字符串。UpgradeCard.cs 不再依赖 enum，改用 string action + float value。**Why:** 学习 Lua 热更概念。**How to apply:** 新增升级只需改 upgrades.lua.txt + ApplyAction 加 case。注意：require 返回模块表需用 DoString("return require 'xxx'") 捕获，不能用 Global.Get 查找。
- [2026-08-14 17:00:45] v1.5 玩法改机+数值平衡+目录清理（2026-08-14）：①火球改为自动定时发射（Physics.OverlapSphere 索敌，无敌人朝面朝方向，间隔1s可升级缩短到0.1s）②近战斩击技能（MeleeCombat.cs 自动半圆范围攻击+SlashEffect.prefab 粒子特效）③UpgradeSystem 重写为 xLua 驱动（upgrades.lua.txt 数据驱动，7个升级选项）④500敌人数值平衡：maxCount=Min(500,100+lv*20)、spawnInterval=Max(0.05,0.3-lv*0.015)、bossInterval=30s、BossHP增长lv*1、玩家HP=200、受击DPS上限40/s+音效冷却0.5s⑤目录清理：删除旧角色模型/重复目录/旧SFX/旧Sprite/旧Player.prefab共20+项，迁移7个文件到规范位置（GUID保持引用）
- [2026-08-17 17:15:07] 玩家闪避翻滚动画已回退（2026-08-17）：Falling To Roll.fbx 资源保留但不再使用。PlayerMovement.cs 中 SetBool("IsDashing") 调用已移除。PlayerController.controller 中 IsDashing 参数、Roll 状态、AnyState→Roll 过渡均已移除。闪避恢复为纯位移无动画状态。

- [2026-08-18 16:20:08] 瓦片地图替换方案已完成（2026-08-18）：①安装 com.unity.2d.tilemap + com.unity.2d.tilemap.extras 3.1.3（Tuanjie 1.9.3 兼容）②RuleTile 的 Default GameObject 字段实现画笔画 2D Sprite 同时自动实例化 3D Prefab，无需转换脚本 ③Tilemap 会覆写 Prefab scale 为 (1,1,1)，必须用自定义 Mesh（顶点尺寸=目标尺寸），不能用 scale 缩放内置 Quad/Cube ④Grid rotation Euler(270,0,0) 使瓦片平铺朝上（XZ 平面）⑤URP Lit 材质创建时必须设 _BaseColor（非 _Color），否则默认白色 ⑥画完后禁用 TilemapRenderer 隐藏 2D 贴图，只显示 3D 瓦片 ⑦cellSize=1.25（原 10 的 1/8），96×96=9216 片铺满 120×120 全 Ground ⑧5 种地形：草地(8100) + 河流(280,3格宽对角线|x-y|<=1) + 石子路(167,T形y=24横贯+x=52纵贯) + 河岸(362,草地紧邻河流过渡带) + 路边(307,草地紧邻石子路过渡带) ⑨每地形 3 种程序化纹理变体(256×256,Perlin噪声+Voronoi)，共 15 张纹理+15 材质，脚本随机分配材质变体增加地貌多样性 ⑩自定义 Mesh 必须包含 UV 坐标否则纹理不显示(TileMesh1_25_UV.mesh) ⑪NavMesh：NavMeshSurface(CollectObjects.All) 挂在根级 NavMeshSurface GO 上，River Prefab 烤入 NavMeshModifier(area=3 NotWalkable)+BoxCollider，河流 area=8 不可行走 walkable=False ⑫RuleTile 的 TilingRule Random 模式只随机 Sprite 不随机 GameObject，由于 TilemapRenderer 已禁用 Sprite 不可见，因此用脚本随机分配材质变体替代 TilingRule Random ⑬RuleTile 的 TilingRule 邻居检测只支持"同/异"不支持"特定地形"，多地形过渡用脚本遍历边界检测实现而非 TilingRule ⑭场景物件迁移：脚本检测每个物件完整包围盒(Renderer.bounds)与河流/石子路格子的重叠，螺旋搜索最近可用草地位置，移动了 19 个 NatureDecoration/SceneProps 子对象。Grid 位置(-60,0,60)。


- [2026-08-17 16:58:49] 设置界面（暂停菜单+音量控制）已集成（2026-08-17）：①PauseMenu.cs（ESC 开关暂停，与升级/游戏结束面板互斥，鼠标光标管理：运行时隐藏+锁定，暂停时显示+解锁）②SettingsManager.cs（主音量/音效音量 Slider，PlayerPrefs 持久化，实时应用）③AudioManager.cs 扩展（SetMasterVolume→AudioListener.volume，SetSFXVolume→遍历 SFX AudioSource，Awake 加 DontDestroyOnLoad 跨场景持久化，Awake 从 PlayerPrefs 加载音量）④PlayerHealth.ShowGameOver() 补充鼠标解锁⑤Canvas 下新增 PauseMenuPanel（继续/设置/重新开始/退出）和 SettingsPanel（主音量 Slider/音效 Slider/返回）⑥安装 Noto Sans SC 作为 TMP 全局 fallback 字体（Assets/Codely/Fonts/），msyh SDF atlas 1024 满了缺字符，fallback 自动补全中文。UI 面板 TMP 文字使用 msyh SDF。⑦PauseMenuTests.cs 15 个测试用例，全部通过（总计 54 测试 0 失败）。
- [2026-08-18 14:19:14] v1.6 图标+鼠标+NavMesh 修复（2026-08-17）：①升级图标改为 Image Sprite（Resources/Icons/ 7张AI生成图片：HealthIcon/SpeedIcon/FireballIcon/MagnetIcon/FireRateIcon/SwordIcon/SlashCooldownIcon），UpgradeCard 从 TMP iconText 改回 Image iconImage，Lua 配置 icon 字段对应文件名 ②UpgradeSystem ShowUpgrades 时 Cursor.visible=true+lockState=None，SelectUpgrade 时恢复隐藏锁定 ③图标位置上移（锚点 0.65-0.90 Y）与标题描述增加间距 ④EnemyMovement.Update 加 navMeshAgent.isOnNavMesh && enabled 检查防止 SetDestination 报错 ⑤RunTestsEditor.cs 加 #if UNITY_INCLUDE_TESTS 包裹防止编译错误
- [2026-08-19 16:00:41] v1.7 图标透明+Ground修复+TJGenerators清理+README更新（2026-08-18）：①升级图标JPEG转PNG透明背景（阈值0.85+0.65-0.85渐进淡出），7张全部替换为AI生成新图+透明处理 ②Ground碰撞体重建（瓦片地图替换方案删除了原Ground，玩家掉落） ③TJGenerators目录完全删除，temple prefab迁移到Assets/Quaternius/quaternius_ultimate_fantasy_rts/DownloadedTemple/ ④SlashEffect.prefab Sparks子对象材质NULL修复（赋slash03_ADD.mat） ⑤MeleeCombat斩击特效旋转Euler(90,0,0)让Mesh平铺环绕主角 ⑥EnemyMovement.Update加isOnNavMesh检查防SetDestination报错 ⑦README项目结构/玩法/功能表全面更新反映v1.5-v1.6改动 ⑧已推送到GitHub（ad6a674, 595文件）
- [2026-08-19 17:22:50] 升级冲击波改动（2026-08-19）：①从 upgrades.lua.txt 移除 shockwave_damage 和 shockwave_radius 两个升级选项（冲击波是升级附带效果，不算主角能力）②UpgradeSystem.ShowUpgrades 改为协程：先触发冲击波（timeScale=1 保持物理正常）→ 等待 expandDuration(0.4s) → 再暂停+显示面板③SelectUpgrade 中移除冲击波触发（已移到面板前）④ApplyAction 移除 IncreaseShockwaveDamage/IncreaseShockwaveRadius 两个 case⑤ShockwaveEffect.Trigger 中 Instantiate 后加 SetActive(true) 修复协程在 inactive GameObject 上启动报错⑥创建 ShockwavePrefab.prefab（圆环Mesh+蓝色透明URP Unlit材质+ShockwaveVFX脚本）并赋给 Player 的 ShockwaveEffect.shockwavePrefab（之前为 NULL 导致无视觉特效）⑦2个 ShockwaveEffectTests 失败是修改前已存在的 EditMode Destroy 问题，与本次改动无关
- [2026-08-19 20:30:04] 冲击波特效修复（2026-08-19）：①材质从 Hidden/Internal-Colored 改为 URP/Unlit（Hidden shader 不渲染）②Mesh 三角形绕序反转使法线朝上③Mesh 顶点加 Y=0.1 厚度避免视锥剔除④expandDuration 从 0.4s 改为 1s⑤UpgradeSystem 等待方式改为手动 Time.unscaledDeltaTime 计时器+Mathf.Min(0.1) 限制单帧上限⑥ShockwaveVFX 协程用 Time.unscaledDeltaTime 替代 Time.deltaTime 防止 timeScale=0 时动画不推进⑦targetScale 移除 *2f 圆环最终大小=radius(6m)
- [2026-08-19 20:51:17] 冲击波击退修复+GitHub推送（2026-08-19）：①击退方式从 Rigidbody.AddForce 改为 NavMeshAgent 位移协程（敌人无 Rigidbody 组件，AddForce 直接跳过）②KnockbackAgent 协程：临时禁用 NavMeshAgent → Vector3.Lerp 位移 3m（缓出曲线）→ NavMesh.SamplePosition 确保落点在 NavMesh 上 → 重新启用 Agent③knockbackForce 字段替换为 knockbackDistance(3) + knockbackDuration(0.3s)④ShockwaveEffectTests 更新匹配新字段⑤已推送到 GitHub（02bfce6, 16文件+823行）

### Reference

